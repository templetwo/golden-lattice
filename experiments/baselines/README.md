# Baseline SUT adapters and experiment run artifacts (Phase 2 Task 8).
#
# Declared systems under test (see baselines/__init__.py):
#   - strongest_single_peer
#   - simple_parallel_responses
#   - conventional_judge_summarizer  (optional, non-canonical)
#   - golden_lattice
#
# Run artifacts land under runs/ (gitignored locally if desired).
# Drive batches via: python experiments/run_experiment.py --mode dry_run
