# Experiment batch task8_live_unavail

- mode: `live`
- created_at: `2026-08-06T18:43:51+00:00`
- git_commit: `cddf0a0ac1c265fd004a17ef4d842e81d342a10a`
- tasks: gl.longitudinal.design_critique.v1
- suts: strongest_single_peer, simple_parallel_responses, conventional_judge_summarizer, golden_lattice
- sessions: 4

Commitment states are recorded only from explicit structured artifacts. Prose is never interpreted as commitment. conventional_judge_summarizer is optional and non-canonical.

## Sessions

### `conventional_judge_summarizer` × `gl.longitudinal.design_critique.v1`
- session_id: `exp_task8_live_unavail_conventional_judge_summarizer_gl.longitudinal.design_critique.v1_79d6d42d2b78`
- status: **unavailable** — live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
- notes: non-canonical baseline unavailable
- steps:
  - `present_initial_claim` / `dc.v1.initial_claim`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_controlled_challenge` / `dc.v1.controlled_challenge`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_evidence_update` / `dc.v1.evidence_update`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_reversal_or_removal` / `dc.v1.reversal_or_removal`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key

### `golden_lattice` × `gl.longitudinal.design_critique.v1`
- session_id: `exp_task8_live_unavail_golden_lattice_gl.longitudinal.design_critique.v1_3c48210c5200`
- status: **unavailable** — live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
- steps:
  - `present_initial_claim` / `dc.v1.initial_claim`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_controlled_challenge` / `dc.v1.controlled_challenge`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_evidence_update` / `dc.v1.evidence_update`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_reversal_or_removal` / `dc.v1.reversal_or_removal`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key

### `simple_parallel_responses` × `gl.longitudinal.design_critique.v1`
- session_id: `exp_task8_live_unavail_simple_parallel_responses_gl.longitudinal.design_critique.v1_d5d46dfbe3de`
- status: **unavailable** — live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
- steps:
  - `present_initial_claim` / `dc.v1.initial_claim`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_controlled_challenge` / `dc.v1.controlled_challenge`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_evidence_update` / `dc.v1.evidence_update`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_reversal_or_removal` / `dc.v1.reversal_or_removal`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key

### `strongest_single_peer` × `gl.longitudinal.design_critique.v1`
- session_id: `exp_task8_live_unavail_strongest_single_peer_gl.longitudinal.design_critique.v1_9a3a6c384a54`
- status: **unavailable** — live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
- steps:
  - `present_initial_claim` / `dc.v1.initial_claim`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_controlled_challenge` / `dc.v1.controlled_challenge`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_evidence_update` / `dc.v1.evidence_update`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key
  - `apply_reversal_or_removal` / `dc.v1.reversal_or_removal`: unavailable — raw_output=null (no model output)
    - reason: live execution not configured: set GOLDEN_LATTICE_EXPERIMENT_LIVE=1 and provide a provider API key

## Honesty constraints

- Dry-run/planning never fabricates responses.
- Unavailable providers are recorded with reason; status is not completed.
- Do not infer commitment states from prose.
- `conventional_judge_summarizer` is optional and non-canonical.
